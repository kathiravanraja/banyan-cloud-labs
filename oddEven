/*check whether a number is even or odd */

#include<stdio.h>

int main()
{

  int n;
  
   scanf("%d", &n);
  
  if(n%2==0)
      printf("\n The number is even");
  else
      printf("\n The number is odd");

  return 1;
  }